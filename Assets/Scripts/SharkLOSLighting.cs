//Cooper Spring, 11/9/2020, script for visual shark LOS
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SharkLOSLighting : MonoBehaviour
{



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //RaycastHit2D hit = Physics2D.Linecast(transform.position,, 1 << LayerMask.NameToLayer("Action"));
    }
}
